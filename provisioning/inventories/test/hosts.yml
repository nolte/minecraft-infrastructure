all:
  vars:
    restic_version: 0.9.4
    sshd_AllowUsers:
      - "{{ ansible_user }}"
      - "vagrant"
  children:
    minecraftgameserver:
      hosts:
        mcservice:
          ansible_port: 22
          ansible_connection: local
          ansible_user: root
          ansible_become: yes
